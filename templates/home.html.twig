{% extends 'base.html.twig' %}

{% block description 'Profitez des vidéos et de la musique que vous aimez, mettez en ligne des contenus originaux, et partagez-les avec vos amis, vos proches et le monde entier.' %}
{% block title 'Accueil' %}

{% block content %}
	{# Template bootstrap #}
	<div class="album py-5 bg-body-tertiary">
		<div class="container">
			<div
				class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
				{# {% for video in videos %} #}
				<div
					class="col">
					{# <a href="{{ path('app_video_show', {'id': video.id}) }}" class="text-dark" style="text-decoration: none;"> #}
					<div
						class="card shadow-sm" style="min-height: 700px;">
						{# <img src="{{ video.thumbnail }}" class="bd-placeholder-img card-img-top" width="100%" height="225"> #}
						<div class="card-body p-5">
							{# <h3 style="font-weight: bold;" class="mb-3">{{ video.title }}</h3> #}
							{# <small class="card-text text-secondary">{{ video.description }}</small> #}
								<div class="d-flex justify-content-between align-items-center"> <div
									class="btn-group mt-5">{# <a href="{{ video.url }}" type="button" class="btn btn-sm btn-outline-secondary">Regarder la vidéo</a> #}
								</div>
								<small class="text-body-secondary">9 mins</small>
							</div>
						</div>
					</div>
				</a>
			</div>
			{# {% endfor %} #}
		</div>
	</div>
</div>{% endblock %}
